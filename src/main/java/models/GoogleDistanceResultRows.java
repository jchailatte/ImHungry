package models;

import java.util.ArrayList;

/// Holds the elements of results in each GoogleDistanceResult
public class GoogleDistanceResultRows {
    ArrayList<GoogleDistanceResultElement> elements;
}
