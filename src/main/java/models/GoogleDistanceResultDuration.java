package models;

/// Holds the duration from the G distance matrix API in each GoogleDistanceResultElement in each
/// GoogleDistanceResultRow in each GoogleDistanceResult
public class GoogleDistanceResultDuration {
    String text;
}
